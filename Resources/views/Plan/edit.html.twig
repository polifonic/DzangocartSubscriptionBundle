{% trans_default_domain "dzangocart_subscription" %}

{{ form_start(form) }}

<h3>{% trans %}plan.edit.title{% endtrans %}</h3>

{{ form_errors(form) }}

{% for flashMessage in app.session.flashbag.get('plan.edit') %}
	<div class="alert alert-success">
		{{ flashMessage }}
	</div>
{% endfor %}

{{ form_row(form.name) }}

{{ form_row(form.description) }}

<div class="row">
	<div class='col-sm-6'>
		<strong>{{ 'plan.form.from.label' | trans }}</strong>
		<div id='start_datetimepicker' data-date-format="YYYY-MM-DD">
			{{ form_widget(form.startdatepicker) }}
		</div>
	</div>
	<div class='col-sm-6'>
		<strong>{{ 'plan.form.to.label' | trans }}</strong>
		<div id='finish_datetimepicker' data-date-format="YYYY-MM-DD">
			{{ form_widget(form.finishdatepicker) }}
		</div>
	</div>
</div>

<div style="display: none;">
	{{ form_widget(form.start) }}
	{{ form_widget(form.finish) }}
</div>

{{ form_row(form.save) }}

{{ form_end(form) }}
